// JWT secret is read at runtime from environment.
// This file exists only as a fallback type helper.
// In production always set JWT_SECRET env variable.
export const JWT_SECRET_KEY = 'JWT_SECRET';
